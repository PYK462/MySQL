# 校园二手物品交易系统：数据库设计与实现报告

## 摘要
本报告详细阐述了“校园二手物品交易系统”的数据库设计与实现全过程。面向高校内部由于信息不对称产生的闲置物品交易需求，本系统基于关系型数据库规范化理论，使用MySQL数据库构建。通过需求分析、E-R概念流转、逻辑结构设计到全面实现，本系统解决了传统闲置群信息碎片化的问题。

## 一、 引言
随着高校学生规模扩大，每学期产生大量闲置书籍、电子产品与生活用品。通过系统化的二手平台，能有效盘活校园闲置资源。要求底层数据库能够支撑高频发帖、多条件检索（价格/时间/分类等）、订单状态一致性及互动留言功能，同时保证数据操作的原子性与完整性。

## 二、 需求分析
1. **用户属性管理**：需要区别买方、卖方和管理员。提供注册、资料维护等功能。
2. **物品发布流转**：从发布、修改、到由于下单导致的“锁定/下架”需具有严谨的状态机。
3. **订单交易核算**：涉及交易金额存储及支付、发货等流转时间点的记录。
4. **互动交流保障**：需要在单一物品维度下，提供父子结构的留言板。

## 三、 概念与逻辑结构设计
经过实体间关系梳理，提炼出核心5张表。设计上严格遵守3NF：
- 物品表 (`Items`) 通过 `SellerID` 与 `Users` 建立强关联，通过 `CategoryID` 指向分类。
- 订单表 (`Orders`) 引入冗余设计中的权衡——剥离出 `BuyerID` 和 `SellerID` 及独立快照金额 `TotalAmount`，避免后期商品价格变更对历史订单造成影响。这在电商交易系统中是保护数据历史不可篡改的必要设计。

*(详见《数据库设计方案.md》资料及E-R图部分)*

## 四、 数据库实现与核心操作说明
基于MySQL 9.6 标准（在实践中对兼容性进行了处理），创建了建库DDL与表模型：

1. **主外键约束与级联**：通过`FOREIGN KEY`确保了如商品被强制删除时留言表或订单流转的逻辑一致性预防。
2. **订单检索SQL实现场景**：
```sql
SELECT o.OrderID, o.TotalAmount, i.ItemName, u.UserName AS SellerName 
FROM Orders o
INNER JOIN Items i ON o.ItemID = i.ItemID
INNER JOIN Users u ON o.SellerID = u.UserID
WHERE o.BuyerID = 'U002' AND o.OrderStatus = 1;
```
3. **商品留言树形查询测试**：
利用 `LEFT JOIN` 实现同一表(`Comments`)自连接，将父留言与子回复平铺展示。

## 五、 测试与分析
针对已录入的测试数据进行以下性能与安全性验证：
1. **实体完整性测试**：尝试插入已有的 `UserID` ，数据库正确提示 `Duplicate entry` 防御。
2. **参照完整性测试**：尝试删除分类包含在售商品的 `Category` 记录，由于约束，引擎正常拦截。
3. **索引性能分析**：在多表联合搜索时，根据EXPLAIN解析，`idx_items_status` 成功介入，使得首页全局扫描(ALL)降级为索引范围扫描(Ref)。

## 六、 总结与心得体会
通过本次数据库项目实战，我将课堂所学的DDL、DML以及范式理论真正落地为实际服务。
- 深化了认知：特别在订单表金额设计时，认识到范式非绝对约束（有时需保留静态快照避免历史篡改）。
- 工程化思维：主外键虽然在概念上完美，但在实际高并发中可能存在锁表风险，这也让我在建立索引与逻辑层数据约束之间找到了折中点设计思维。
- 收获了完整的项目掌控力，涵盖从业务抽象、建模到SQL底层的全流程建设。
